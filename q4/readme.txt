To compile and run, enter the following in the linux shell:
$ bash compile_run.sh

To just compile and see the warnings, enter:
$ bash compile.sh

To compile, run and compare efficiency with 1, 2 and 4 threads, enter:
$ bash compile_run_cmp.sh